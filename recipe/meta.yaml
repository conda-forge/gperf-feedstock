{% set version = "3.3" %}
{% set sha256 = "fd87e0aba7e43ae054837afd6cd4db03a3f2693deb3619085e6ed9d8d9604ad8" %}

package:
  name: gperf
  version: "{{ version }}"

source:
  url: http://ftp.gnu.org/pub/gnu/gperf/gperf-{{ version }}.tar.gz
  sha256: {{ sha256 }}

build:
  number: 2

requirements:
  build:
    - gnuconfig  # [unix]
    - {{ compiler('c') }}          # [unix or target_platform == "win-arm64"]
    - {{ compiler('cxx') }}        # [unix or target_platform == "win-arm64"]
    - {{ stdlib('c') }}            # [unix or target_platform == "win-arm64"]
    - {{ compiler('m2w64_c') }}    # [win and target_platform != "win-arm64"]
    - {{ compiler('m2w64_cxx') }}  # [win and target_platform != "win-arm64"]
    - {{ stdlib('m2w64_c') }}      # [win and target_platform != "win-arm64"]
    - make  # [target_platform != "win-arm64"]
    - autotools_clang_conda  # [target_platform == "win-arm64"]
    - m2-base  # [win and target_platform != "win-arm64"]
    - m2-bash  # [win and target_platform != "win-arm64"]
    - m2-sed   # [win and target_platform != "win-arm64"]
    - m2-diffutils  # [win and target_platform != "win-arm64"]
    - sed      # [osx]

test:
  requires:
    - {{ compiler('c') }}  # [target_platform == "win-arm64"]
    - {{ stdlib('c') }}  # [target_platform == "win-arm64"]
  files:
    - keywords.gperf  # [target_platform == "win-arm64"]
    - check_lookup.c  # [target_platform == "win-arm64"]
  commands:
    - gperf --help         # [unix]
    - gperf --version      # [unix]
    - gperf.exe --help     # [win]
    - gperf.exe --version  # [win]

about:
  home: https://www.gnu.org/software/gperf/
  dev_url: http://savannah.gnu.org/projects/gperf/
  doc_url: https://www.gnu.org/software/gperf/manual/gperf.html
  license: GPL-3.0-or-later
  license_file: COPYING
  summary: GNU gperf is a perfect hash function generator.

extra:
  recipe-maintainers:
    - mbargull
    - tfors
    - isuruf
    - luhenry
